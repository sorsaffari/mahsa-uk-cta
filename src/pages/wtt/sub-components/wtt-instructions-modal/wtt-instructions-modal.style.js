import { Alert } from '@mantine/core';
import styled from 'styled-components';

export const SCAlert = styled(Alert)`
  p {
    margin: 0;
    color: #776ef3;
    font-size: 0.75rem;
  }

  .helper-text {
    font-size: 0.625rem;
  }
`;
